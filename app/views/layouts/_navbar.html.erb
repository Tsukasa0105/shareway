<header class="mb-4">
  <nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="col-1 navbar_logo">
    　<a class="navbar-brand" href="/"><i class="fab fa-slideshare"></i>ShareWay</a>
    </div>
    <div class="col-1 navbar_user_image">
      <% if logged_in? %>
        <p class="text-center"><%= render 'users/image', user: current_user %></p>
        <p class="text-dark text-center"><font size="2"><%= link_to current_user.name, root_path %></font></p>
      <% end %>      
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end col-9 flex-column" id="navbarSupportedContent">
      <ul class="navbar-nav w-100 nav-justified">
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">ユーザー</a>
            <div class="dropdown-menu">
              <div class="dropdown-item"><%= link_to 'すべてのユーザー', users_path %></div> 
              <div class="dropdown-item"><%= link_to 'フォロー', followings_user_path(current_user) %></div>
              <div class="dropdown-item"><%= link_to 'フォロワー', followers_user_path(current_user) %></div>
            </div>
          <li class="nav-item dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">趣味</a>
            <div class="dropdown-menu">
              <div class="dropdown-item"><%= link_to '一覧', hobbies_path %></div> 
              <div class="dropdown-item"><%= link_to '気になる', like_hobbies_user_path(current_user) %></div>
            </div>
          <li class="nav-item dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">楽しみ方</a>
            <div class="dropdown-menu">
              <div class="dropdown-item"><%= link_to '気になる', ways_path %></div> 
              <div class="dropdown-item"><%= link_to '満足', satisfied_ways_user_path(current_user) %></div>
            </div>
          <li class="nav-item dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">アカウント</a>
            <div class="dropdown-menu">
              <div class="dropdown-item"><%= link_to '設定', edit_user_path(current_user) %></div> 
              <div class="dropdown-item"><%= link_to 'ログアウト', logout_path, method: :delete %></div>
            </div>
          <li class="nav-item"><%= link_to 'Q&A', how_to_use_toppages_path %></li>
        <% else %>
          <li class="nav-item"><%= link_to 'Signup', signup_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Login', login_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>
