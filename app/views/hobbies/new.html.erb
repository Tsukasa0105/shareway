  <div class="row">
    <aside class="offset-sm-1 col-sm-10 offset-sm-1">
        <%= form_with(model: @hobby, local: true) do |f| %>
          <div class="form-group">
            <%= f.label :name, '新しい趣味を追加' %>
            <%= f.text_field :name, class: 'form-control' %>
            <%= f.submit '追加', class: 'btn btn-primary btn-block' %>
          </div>  
        <% end %>
    </aside>
  </div>